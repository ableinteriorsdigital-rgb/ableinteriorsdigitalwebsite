import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Windows | uPVC & Aluminium Windows",
  description:
    "Explore Able Interiors & Windows' premium uPVC and aluminium window solutions — casement, sliding, tilt & turn, bay, and fixed windows with 20-year warranty.",
  keywords: ["uPVC windows", "aluminium windows", "sliding windows", "casement windows", "bay windows", "tilt and turn windows", "window installation Chennai"],
};

export default function WindowsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
